# 系统盘备份 / System disk misc

本目录存放从运行环境系统盘（如 `/root`）备份的、与项目相关的配置示例，便于复现或迁移环境。

- `bashrc_example`：从 `/root/.bashrc` 复制的 shell 配置示例
- `gitconfig_example`：从 `/root/.gitconfig` 复制的 git 配置示例

如需加入其它系统盘上的代码或文件，可将文件放到此目录或项目根目录下，再 `git add` 并 `git commit`。
